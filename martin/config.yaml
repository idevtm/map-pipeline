listen_addresses: 0.0.0.0:3000
cors: true

mbtiles:
  sources:
    basemap: /data/published/current.mbtiles

styles:
  sources:
    basemap: /styles/style.json
    basemap-labels: /styles/style-labels.json
